using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerInteract : MonoBehaviour
{
    private Transform npc;

    private void Update()
    {
        bool interactButtonHeld = pla
        if ()
        float interactableRange = 2f; // how close player needs to be to npc 
        Physics.OverlapSphere(transform.position, interactableRange);
    }
}
